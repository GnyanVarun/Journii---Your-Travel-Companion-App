class Trip {
  final String id;
  final String title;
  final String description;
  final DateTime createdAt;

  Trip({
    required this.id,
    required this.title,
    required this.description,
    required this.createdAt,
  });
}
